# Changelog

## Unreleased

## 0.1.16

- 顶栏增加导出：PDF（系统浏览器打印存盘；Cursor 内置窗口改为下载可打印 HTML，避免崩溃）与 PNG 图片
- 修正 PDF 分页：长代码/表格可跨页，流程图缩进单页
- 查看原文图标改为代码括号，导出改为独立菜单

## 0.1.15

- 流程图编辑页右侧预览可就地滚轮 / 按钮缩放，不必再开灯箱
- 观看灯箱放大后可直接进入流程图编辑
- 顶栏改为「查看原文」：原文不再挡住大纲，点击标题可跳到对应源码
- 工作室内置样例改为框架图与时序图
- 预览「+」插入后切到编辑时保持滚动位置和焦点
- 修复代码块展开编辑时源码不可读、切模式跳动
- 流程图改为独立编辑对话框，预览态也可改源码并插入模板片段

## 0.1.14

- 工作室侧栏直接列出一份内置样例，去掉「试读示例」按钮
- 预览和编辑切换时停在当前阅读位置，不再跳回开头
- 编辑态代码块与预览同高，避免切模式时页面跟着跳
- 大纲改为 Vditor 左侧栏，不再做成浮动菜单；开关按钮固定漂在阅读区左上角，不占正文、不随滚动走；文档更新后大纲跟着刷新
- 大纲改为层级目录图标，编辑方式改为叠页图标，避免和「编辑 / 对齐」混淆
- 编辑时去掉全屏和工具栏字数
- 预览和编辑时，悬停块左侧出现「+」，可插入标题、列表、代码、表格、公式、流程图等（不必手写 Markdown）
- 表格：插入时可选择行列数（不再固定 3×3）；编辑时选中单元格可增删行列；工具栏表格按钮在 Vditor 灰掉时仍可点开尺寸网格
- 图片改为插入网络地址，不再从本地文件拷到文档旁边
- 流程图预览与灯箱尺寸对齐预览态；工具栏悬停或聚焦时才显示
- VS Code 顶栏去掉「已同步 / 未保存」：标签页圆点已经表示脏状态，底栏只留字数

## 0.1.13

- 商店介绍补上所见即所得、纸面、排版、表格、Mermaid、公式

## 0.1.12

- 商店介绍改为中 / 英 / 西三语短文

## 0.1.11

- 商店介绍动画改为从 https://molan.guoyoutech.cn/intro.gif 加载，不再走 GitHub raw

## 0.1.10

- 介绍动画改为英文演示：阅读、调字号、流程图放大、表格编辑
- 插件顶栏最右改为设置齿轮，纸面放进设置；工作室设置可一键唤起 VS Code / Cursor 安装扩展

## 0.1.9

- 预览顶栏可调节字号、行距、段距、字距，选择记在本机

## 0.1.8

- 主页改为 https://molan.guoyoutech.cn/extension.html（含 Open VSX / VS Code 安装链接）

## 0.1.7

- 文档内查找：`Cmd/Ctrl+F` 打开查找栏，高亮匹配并跳转；VS Code 下也会接管工作区的查找快捷键

## 0.1.6

- 表格：窄表按内容收缩（不再右边留白底）；宽表限制在纸面宽度内并自动换行
- 宽表按每列内容的自然宽度分配空间；含长英文或行内代码的列保留更大的最小宽度
- 顶部「预览 / 复制原文」始终和标题同一行，窄窗口时标题省略，按钮不再掉到下面

## 0.1.4

- 工具条图标：用带 nonce 的脚本加载 SVG 精灵图，避免 webview CSP 拦掉 Vditor 的内联注入

## 0.1.3

- 打开 Markdown 默认进入编辑，不再先闪一下再切到预览
- 顶部格式工具条改为 flex 布局，避免被裁掉看不见
- 编辑与预览共用全宽纸面，避免切换时宽度跳动
- 宽表格在纸面最大宽度处自动换行；窄表格背景不再铺满整行

## 0.1.2

- 商店介绍改为面向普通用户
- 修正商店 README 截图地址（指向 `apps/vscode-molan/media/`）

## 0.1.1

- 打开 Markdown 默认进入预览，点「编辑」再改
- 只看不改时，关闭文件不再询问保存
- 安装包更小

## 0.1.0

- 首次发布：点击 Markdown 用墨览所见即所得打开
- 支持 `.md` / `.markdown` / `.mdown` / `.mdx`
- Mermaid 流程图观看（拖动 / 缩放）、源码编辑、复制代码或图片
- `Cmd/Ctrl+S` 写回原文件

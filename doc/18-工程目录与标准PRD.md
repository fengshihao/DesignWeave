# 工程目录与标准 PRD

> 状态：已拍板 · 2026-08-22 · 2026-09-01 改单文件 `PRD.md` · **2026-09-02 托付交互改听 [19](./19-对文档批注托付.md)** · **2026-09-04 三角色 / 文件夹 / 跟上听 [20](./20-角色文件夹与变更跟上.md)**  
> 本文仍管：目录即工程、单文件 PRD、代码目录批准、单页全屏。  
> **作废：** §5 清晰度门闩拦发送、§7 档位 Tab、§9 四档 AI 全链路——改由 [19](./19-对文档批注托付.md) 验收。  
> **§1 角色、§2 里 `arch/` `qa/`、§8 一工程一写者** 改听 [20](./20-角色文件夹与变更跟上.md)。  
> 安装形态、Git 词汇等未改口的约束仍听 [00](./00-产品重定位.md)、[07](./07-客户端架构.md)、[11-3](./11-设计稿-3-Git与文档同步.md)、[12](./12-实现前决策.md)。  
> 人若改口：先改 [19](./19-对文档批注托付.md) / [20](./20-角色文件夹与变更跟上.md) / 本文和 [03](./03-待确认问题.md)，再动代码。

本文改写此前几条：只有架构师建工程、每工程自选文档根并勾代码仓、导入先保真不重排、多页首页与分步向导、多文件 01–05 文档包。

---

## 1. 角色

三角色、谁写哪个文件夹、测试不能建工程：见 [20](./20-角色文件夹与变更跟上.md) §1。

架构师不再给每个工程选文档目录，也不再给每个工程勾代码仓。根目录未设定时，产品经理不能建工程；空纸面上只留一句，不做成说明页。测试不建工程。

**删除：** 只有架构师能删；动作 = 删掉该子文件夹（含 `.git`）。产品经理和测试不能删。

**可见性：** 根目录下所有工程全员可见。

界面写「产品经理」「测试」。代码角色值：`designer`、`tester`。

---

## 2. 目录即工程

工程列表不靠数据库。架构师设定 `workspaceRoot` 之后，每个子文件夹就是一个工程。丢掉 SQLite 仍能扫盘恢复列表；丢掉文件夹等于丢掉工程。

```text
<workspaceRoot>/                    # 架构师在设置里选定
  设置页夜间模式/                    # 文件夹名 = 工程名（允许中文）
    meta.md
    product/
      PRD.md
      待办.md
      问题.md
      gaps.md
      import/
        original.md
    eng/
      方案.md
      待办.md
      问题.md
      跟上.md
    qa/
      测试.md
      待办.md
      问题.md
      跟上.md
    .git/
    .dw/
  负一屏天气卡片/
    ...
```

固定三个文件夹、谁写哪份、侧栏怎么列：见 [20](./20-角色文件夹与变更跟上.md) §2。

- 列表 = 扫描根目录下带 `meta.md` 的子目录。
- 文件夹名用工程标题，去掉 `/ \ : * ? " < > |`。重名则 `名称-2`。
- `meta.md` 写稳定 `id`（短 nanoid），路由用 id，改名不炸链接、不打散编辑权。
- SQLite 只留用户、锁、runs；工程列表可留扫盘缓存，丢掉也能重建。
- 每个子文件夹仍是独立 Git 文档仓：保存 ≠ 记入版本；AI 一轮成功自动 commit，作者名 `AI`。记版按文件夹 stage，见 [20](./20-角色文件夹与变更跟上.md) §3。

---

## 3. 标准 PRD：单文件 PRD.md

**2026-09-01 拍板：** 产品经理只改一篇 `product/PRD.md`。各章用 `##` 标题分隔，不拆成 01–05 多文件。AI 一次 Read 成本低，Git diff 仍可按章节看。

| 文件 | 谁写 | 写什么 |
|------|------|--------|
| `product/PRD.md` | **产品经理**（AI 共创时可写回对应章节） | 一句话目标 + 五章：`## 背景与目标` / `## 用户故事` / `## 交互与体验` / `## 规格与约束` / `## 验收` |
| `product/gaps.md` | AI | 待补齐清单，对应到 PRD.md 章节。不对外展示给最终读者 |
| `meta.md` | 系统 + AI（clarity 过关时） | id、title、owner、source（template / import）、phase、clarity |
| `product/import/original.md` | 系统（导入路径） | 粘贴 / 上传的原文，只读参照 |
| `eng/方案.md`、`qa/测试.md`、各文件夹 `跟上.md` | 见 [20](./20-角色文件夹与变更跟上.md) | |

用户故事固定形态（写在 `PRD.md` 的 `## 用户故事` 下）：

```text
### US-001 （标题）

- 作为：
- 我想要：
- 以便：
- 验收：
  - [ ] （可观察、可判定的一条）
```

模板放 `packages/templates/prd-pack/PRD.md`，不要写在代码字符串里。每节给填写说明 + `（待补充）`。

---

## 4. 两条创建路径

同一纸面上打开新建面板：工程名 + 「从模板填写」或「导入 PRD」。不选文档目录，不选代码仓。创建后打开 `product/PRD.md`。

### 路径 A：模板

1. 建文件夹，拷入三个固定目录（见 [20](./20-角色文件夹与变更跟上.md) §2），`source: template`，`phase: filling`。
2. 人在墨览把各 `##` 节填完。这阶段不开放可行性。
3. 跟设计类 AI 对话之前，先跑「检查清晰度」。AI 只读 `PRD.md`，按检查表标清晰 / 缺失 / 含糊，写入 `gaps.md`，一次追问 1～3 个问题，人答完写回 PRD.md 对应章节。
4. 必填节不再是占位、没有会改设计的含糊句，才把 `meta.md` 的 `clarity: ready`，解锁共创 / 拷问 / 可行性。

「清晰」≠「能开发」。标准是无歧义、可开始设计讨论。占位符、纯「等」「看情况」、用户故事缺验收、主流程有头无尾、规格和故事打架，都不算过。

### 路径 B：导入

1. 粘贴或上传（不做 URL 抓取）。原文只进 `import/original.md`。
2. 立刻做一次规范化：按 PRD.md 五章拆写进对应 `##` 节，不编造事实；对不上的进 `gaps.md`，对应节留 `（待补充）`。
3. 默认档位是「检查清晰度」（`clarify`）。后续对话仍先看 `gaps.md`，缺的就要求人补。
4. 同样要过 `clarity: ready` 才解锁可行性。共创可以边补边写；发送时若仍有 P0 缺口，必须先问，不能假装已经齐。

---

## 5. 清晰度门闩

现有三档之上加 **检查清晰度**（`clarify`）。

| 阶段 | 可用档位 |
|------|----------|
| `filling` / `imported` / `clarifying` | 仅检查清晰度（「帮我把这一节写清楚」也走这套提示词） |
| `ready` | 共创、拷问、可行性 |

可行性还要求架构师至少批准过一个代码目录。未就绪时顶栏只有一点状态，不铺横幅。发送关卡沿用 [12](./12-实现前决策.md)：持编辑权、不在编辑态、git 干净、没有进行中的一轮。

检查表覆盖：背景与用户、目标与非目标、至少 1 条完整用户故事、至少 1 条主流程、入口、规格与 OEM 约束、整包验收。导入路径额外对照 `import/original.md`。

OEM 必问维度仍听 [06](./06-OEM产品经理场景与实现决策.md) §5。

---

## 6. 代码目录：扫入、批准、AI 选用

代码目录是**服务级资源**，与产品经理工程脱钩，不写进工程 `meta.md`。

**来源只有 `~/.claude.json`。** 设置里扫入 Claude 已登记的工程，架构师批准其中一部分。不浏览磁盘、不手补路径、不把扫到的默认全开。再扫一次时，新出现的默认未批准。取消批准即从 AI 可用集合拿掉。全部只读。

产品经理创建、打开、对话都不出现目录列表。需要读代码时，AI 从已批准的目录里自己识别：

1. **目录卡**（可重建）：已批准目录的文件夹名、路径、README / 清单头几行。
2. **本轮选用**：先对照目录卡挑若干个，再只对选中的 Read / Grep。问产品问题消歧，不问「要用哪个 Git 仓库」。
3. **写回**：用了哪些、为什么，写进 `调研.md`。这是 AI 产出，不是创建必填。

一个都没批准时，可行性不可用，发送时说一句「架构师还没批准代码目录」。Agent 的 `additionalDirectories` 只用本轮选中的，不要把全部已批准目录灌进每一次 run。禁止改业务代码。

---

## 7. 界面：单页、全屏、对齐墨览

对齐墨览指空间和发现方式，不是再做一遍编辑器。Web 仍 inline 挂墨览，保留 `reader-header`（见 [16](./16-墨览现状.md)）。

打开就是全视口。主区即纸面。次要能力用 icon、菜单、浮层发现，不把每个按钮和提示写在脸上。

```text
同一壳 · 全视口
┌ 细 rail（可收成 icon）─┬ 墨览纸面铺满 ─────────────────────┐
│ 当前工程的文件           │  reader-header：篇名 · 保存态 · ··· │
│                          │                                     │
│                          │  正文（唯一大面积）                  │
│                          └────────────── 托付底条（收起） ─────┘
└ 设置 / 用户 / 新建 / 切换工程 = 同一壳上的浮层 ───────────────┘
```

| 面 | 怎么出现 | 不要做成 |
|----|----------|----------|
| 工作台 | 默认；登录后进最近工程，没有则空纸面 | 先落到窄列表站 |
| 换工程 | 菜单「切换工程」浮层 | 侧栏常驻工程列表；回「首页」再跳 |
| 新建 | 菜单「新建工程」→ 轻面板：名称 + 模板 / 导入 | 分步向导、选目录 |
| 设置 / 用户 | 架构师从 `···` 打开浮层 | 独立后台表单页 |
| 版本 / 导入 / 对比 | 抽屉或遮罩 | 常驻按钮条 |
| 登录 / 首次建号 | 允许单独一面，仍全屏少 chrome | 小卡片旧后台登录 |

纸面占主：去掉站点 max-width、外圈厚垫、中栏第二层圆角壳。文件名和保存态只出现在墨览顶栏一处。AI 是托付浮层（收起 / 半幅 / 铺满），人改时收成底条。视觉继续用宣纸 / 墨夜 / 终端 / 胭脂，不上仪表盘风。

| 能力 | 默认看见 | 怎么发现 |
|------|----------|----------|
| 写文档 | 纸面 | 打开即是 |
| 跟 AI 说一句 | 底条一个输入 | 展开才见档位、进度 |
| 清晰度 | 顶栏一点状态 | 点开或被拦住时才看缺口 |
| 代码仓 | 无 | 架构师设置里批准；结论在调研里 |
| 记入版本 / 对比 / 撤销 | 无 | header `···` 或「版本」 |
| 导入、删工程、解除编辑权 | 无 | 菜单 |
| 待跟上 | 自己能写的文件夹旁一点 | 点开 `跟上.md`；见 [20](./20-角色文件夹与变更跟上.md) §4 |

---

## 8. 编辑权按文件夹

一工程一写者作废。锁、人 XOR AI、发送关卡的范围改成文件夹，见 [20](./20-角色文件夹与变更跟上.md) §3。需求改完如何通知研发 / 测试跟上，见同文 §4。

`project_id` 仍用 `meta.md` 的稳定 id。界面只留一句短提示。

---

## 9. 下一刀（已改口）

2026-09-02 起下一刀不是四档全链路，而是 [19](./19-对文档批注托付.md)：系统提示词可编辑 + 预览态圈选托付。

横切仍有效、且 19 继续沿用的：发送关卡、Run 生命周期、纸面跟随、真实模型、AI 成功自动记版。清晰度门闩拦发送、档位解锁、可行性必选代码目录——作废。

### 9.1–9.4 历史（不要按它实现）

当时计划是：模板 / 导入 → clarify → 共创/拷问 → 可行性，四档从「能点」到「能日用」。分档表、实现顺序已迁到 19 的 G1–G7。旧稿留在 git 历史里即可。
